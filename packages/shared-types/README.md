# @maxian/shared-types

跨包共享类型定义。同时被 `@maxian/core`、`@maxian/server`、`@maxian/sdk`、`@maxian/ui` 引用。

## 状态

🚧 **占位阶段** — 目前只含 `PROTOCOL_VERSION`。

## 将承载的类型

- 协议版本号
- SSE 事件 discriminated union
- HTTP 请求/响应 schema
- 租户上下文
- 错误码枚举
